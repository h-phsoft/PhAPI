-- Ported from Oracle: db/01-Copy/0020_MNG_0510_ProcFunc.SQL

-- Generated by scripts/db-port/translate.js for mysql.

-- Tables carry their Oracle synonym name; see PORTING-REPORT.md.

SET FOREIGN_KEY_CHECKS = 0;
SET SESSION sql_mode = CONCAT(@@sql_mode, ',NO_AUTO_VALUE_ON_ZERO,PIPES_AS_CONCAT');
USE phsoftme_erp_demo;


-- TODO(port): function is not supported on mysql (source line 1).
-- Original Oracle statement:
--   CREATE OR REPLACE FUNCTION Get_Base_Currency_Id RETURN Number IS
--   Begin
--     Return 2;
--   END Get_Base_Currency_Id;

-- TODO(port): function is not supported on mysql (source line 6).
-- Original Oracle statement:
--   CREATE OR REPLACE FUNCTION Get_Currency_HRate(dDate Date) RETURN Number IS
--     nRate Number := 50;
--   Begin
--     Select Max(Rate)
--       Into nRate
--       From Mng_CurnHist
--       WHERE HDate=(SELECT MAX(HDate) FROM Mng_CurnHist WHERE HDate<=dDate);
--     Return nRate;
--   END Get_Currency_HRate;
