-- Ported from Oracle: db/01-Copy/0010_CPY_0610_TRIG.SQL

-- Generated by scripts/db-port/translate.js for postgres.

-- Tables carry their Oracle synonym name; see PORTING-REPORT.md.

\connect phsoftme_erp_demo


-- REVIEW(port): trigger translated mechanically; verify before use.

CREATE OR REPLACE FUNCTION Cpy_TBID_Cod_Unit_fn() RETURNS TRIGGER AS $$
Declare
Begin
  If NEW.Id IS NULL Then
    Select nextval('Cpy_Cod_Unit_Seq')
      Into NEW.Id;
  End If;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS Cpy_TBID_Cod_Unit ON Cpy_Cod_Unit;
CREATE TRIGGER Cpy_TBID_Cod_Unit
  BEFORE INSERT
  ON Cpy_Cod_Unit
  FOR EACH ROW
  EXECUTE FUNCTION Cpy_TBID_Cod_Unit_fn();

-- REVIEW(port): trigger translated mechanically; verify before use.

CREATE OR REPLACE FUNCTION Cpy_TBID_Cod_Doc_fn() RETURNS TRIGGER AS $$
Declare
Begin
  If NEW.Id IS NULL Then
    Select nextval('Cpy_Cod_Doc_Seq')
      Into NEW.Id;
  End If;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS Cpy_TBID_Cod_Doc ON Cpy_Cod_Doc;
CREATE TRIGGER Cpy_TBID_Cod_Doc
  BEFORE INSERT
  ON Cpy_Cod_Doc
  FOR EACH ROW
  EXECUTE FUNCTION Cpy_TBID_Cod_Doc_fn();

-- REVIEW(port): trigger translated mechanically; verify before use.

CREATE OR REPLACE FUNCTION Cpy_TBID_Perm_fn() RETURNS TRIGGER AS $$
Declare
Begin
  If NEW.Id IS NULL Then
    Select nextval('Cpy_Perm_Seq')
      Into NEW.Id;
  End If;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS Cpy_TBID_Perm ON Cpy_Perm;
CREATE TRIGGER Cpy_TBID_Perm
  BEFORE INSERT
  ON Cpy_Perm
  FOR EACH ROW
  EXECUTE FUNCTION Cpy_TBID_Perm_fn();

-- REVIEW(port): trigger translated mechanically; verify before use.

CREATE OR REPLACE FUNCTION Cpy_TBID_PermPriv_fn() RETURNS TRIGGER AS $$
Declare
Begin
  If NEW.Id IS NULL Then
    Select nextval('Cpy_PermPriv_Seq')
      Into NEW.Id;
  End If;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS Cpy_TBID_PermPriv ON Cpy_PermPriv;
CREATE TRIGGER Cpy_TBID_PermPriv
  BEFORE INSERT
  ON Cpy_PermPriv
  FOR EACH ROW
  EXECUTE FUNCTION Cpy_TBID_PermPriv_fn();

-- REVIEW(port): trigger translated mechanically; verify before use.

CREATE OR REPLACE FUNCTION Cpy_TBID_User_fn() RETURNS TRIGGER AS $$
Declare
Begin
  If NEW.Id IS NULL Then
    Select nextval('Cpy_User_Seq')
      Into NEW.Id;
  End If;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS Cpy_TBID_User ON Cpy_User;
CREATE TRIGGER Cpy_TBID_User
  BEFORE INSERT
  ON Cpy_User
  FOR EACH ROW
  EXECUTE FUNCTION Cpy_TBID_User_fn();
